[target.x86_64-pc-windows-msvc]
rustflags = [
    "-C", "target-cpu=native",
    "-C", "codegen-units=1",
    "-C", "opt-level=3",
]

[build]
jobs = 1  # Reduce parallel jobs to avoid file locking on Windows
