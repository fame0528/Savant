//! Storage module — CortexaDB-based storage engines.
//!
//! The Storage struct in `crate::db` provides the primary database interface.
//! This module is reserved for future storage engine abstraction layers
//! (e.g., read replicas, sharded storage, cold tier).
