# Session Plan: Post-Rollback Recovery & Ollama Integration

**Date:** 2026-03-20
**Status:** IN PROGRESS (Fine-tuning phase)
**Context:** Project was rolled back by another AI agent. This session restores core functionality and integrates Ollama as the default model backend.

---

## Phase 1: Core Recovery (COMPLETE)

### Fixes Applied

| # | Fix | File(s) | Root Cause |
|---|-----|---------|------------|
| 1 | Dashboard WebSocket URLs (3000→8080) | `health/page.tsx`, `marketplace/page.tsx`, `settings/page.tsx` | Other AI changed gateway port but missed 3 pages |
| 2 | Dashboard dev port (3001→3000) | `dashboard/package.json` | Other AI changed port |
| 3 | Logo/images loading | `page.tsx` (2 places) | Loaded from gateway API (not running yet), switched to local `/img/logo.png` |
| 4 | Dashboard asset prefix | `next.config.ts` | `assetPrefix: './'` broke dev mode, now conditional via `TAURI_BUILD=1` |
| 5 | Tauri build commands | `tauri.conf.json` | Updated `beforeDevCommand` and `beforeBuildCommand` |
| 6 | `.env` not loaded in Tauri | `main.rs` | Added `dotenvy::dotenv().ok()` |
| 7 | OR_MASTER_KEY lookup | `swarm.rs:234` | Read from empty `agent_cfg.env_vars` instead of `std::env::var()` |
| 8 | Agent restart loop | `swarm.rs:239-259` | Writing `.env` file triggered SwarmWatcher hot-reload loop |
| 9 | Avatar path resolution | `server.rs:339` | Used relative `PathBuf::from` instead of `config.resolve_path()` |
| 10 | Wrong LLM model | `agent.json` | Missing `model` and `model_provider` fields, fell back to non-existent Claude model |
| 11 | Generic LLM personality | `context.rs:70-78` | Coding prompts unconditionally embedded in system prompt |
| 12 | Identity not loaded | `registry.rs:179` | `identity: None` - SOUL.md/AGENTS.md never read from workspace |
| 13 | Tool tags in chat | `stream.rs` | Added general tag filtering (environment_details, use_mcp_tool, etc.) |
| 14 | Message duplication | `page.tsx` | WebSocket `onmessage` + Tauri event listener both processed events |
| 15 | UI zoom/scaling | `layout.tsx`, `globals.css` | Added viewport meta tag, font-size, overflow:hidden |
| 16 | Placeholder stubs removed | `page.tsx` | Removed heartbeat/memory/context emoji stubs |
| 17 | Agent.json naming | `agent.json` | Changed `agent_id`/`agent_name` from `workspace-savant` to `savant`/`Savant` |
| 18 | Dead code warnings | 6 files | Added `#[allow(dead_code)]` and `.await` |
| 19 | Debug console enhancements | `page.tsx` | Copy button, expand, pause on highlight, dayjs formatting, log level colors |

---

## Phase 2: Ollama Integration (COMPLETE)

### Embedding Service
- [x] Create `ollama_embeddings.rs` with Ollama client + fastembed fallback
- [x] Register module in `utils/mod.rs`
- [x] Update `swarm.rs` to use async `create_embedding_service()`
- [x] Update `memory/async_backend.rs` to use `dyn EmbeddingProvider` trait
- [x] Add `reqwest` to `core/Cargo.toml`
- [x] Verified: `Ollama qwen3-embedding model found, using Ollama embeddings` (2560 dims)

### Config Updates
- [x] Added `vision_model`, `embedding_model`, `[ollama]` section to `savant.toml`

### Vector Data Migration
- [x] Cleared old 384-dim vector data (`data/memory/`) for 2560-dim Ollama migration

---

## Phase 2.5: Reflections System (COMPLETE)

### Changes Made
- [x] Removed `generate_reflection()` call from agent loop (`stream.rs`)
- [x] Removed synthetic reflection handling from heartbeat (`heartbeat.rs`)
- [x] Updated `swarm_insight_history` gateway handler to read from LEARNINGS.jsonl directly
- [x] Updated dashboard to show only diary entries (no streaming insights bleed)
- [x] Wiped LEARNINGS.md and LEARNINGS.jsonl clean for fresh start

### Design
- LEARNINGS.md is Savant's private diary - emergent inner monologue
- Dashboard parses and displays entries, never generates them
- No forced LLM calls for reflections

---

## Phase 3: Dashboard Enhancements (IN PROGRESS)

### Completed
- [x] Auto-scroll with streaming content
- [x] `is_telemetry` field on ChatChunk for event routing
- [x] Collapsible thoughts on assistant messages
- [x] Timestamps on all messages
- [x] Code block copy with working feedback
- [x] Inline code styling (accent color)
- [x] UI tag stripping (environment_details, function calls)
- [x] dayjs time formatting in debug console

### Pending
- [ ] Settings page (model config, ports, paths)
- [ ] FAQ page (provider setup guides)
- [ ] Vision model (qwen3-vl via Ollama)

---

## Phase 4: Tool Access (COMPLETE)

- [x] Savant bypasses CCT policy (`stream.rs:309` - `is_savant` check)
- [x] Sub-agents still go through CCT when added

---

## Phase 5: Settings, FAQ & Vision (NEXT)

### 5.1: Settings Page

**Goal:** Configure all models and system settings from dashboard.

#### Backend
- [ ] `/api/settings` GET - reads savant.toml + agent.json, returns current config
- [ ] `/api/settings` POST - writes savant.toml + agent.json with new values
- [ ] Returns: chat_model, embedding_model, vision_model, ollama_url, gateway_port, heartbeat_interval

#### Frontend (`dashboard/src/app/settings/page.tsx`)
- [ ] **Models section:** Chat model, Embedding model, Vision model (text inputs)
- [ ] **Ollama section:** Server URL, status indicator (green/red)
- [ ] **System section:** Gateway port, heartbeat interval
- [ ] Save button → POST to `/api/settings`
- [ ] Live validation (Ollama reachable, models available)

#### Config Changes
- [ ] Add `vision_model` field to `agent.json`
- [ ] Support runtime model switching in `savant.toml`

---

### 5.2: FAQ Page

**Goal:** Provider setup guides and troubleshooting.

#### Frontend (`dashboard/src/app/faq/page.tsx`)
- [ ] **Getting Started:** How to start, what `npm run dev` does
- [ ] **Ollama Setup:** Install, pull models (qwen3-embedding:4b, qwen3-vl)
- [ ] **OpenRouter:** Get API key, set OR_MASTER_KEY in .env
- [ ] **Models:** Chat vs embedding vs vision
- [ ] **Troubleshooting:** Common errors, debug console usage

---

### 5.3: Vision Model Integration

**Goal:** Savant understands images via Ollama qwen3-vl.

**Note:** Ollama HTTP client already wired in (embeddings use same pattern).

#### Backend
- [ ] Create `crates/core/src/utils/ollama_vision.rs`
  - `OllamaVisionService` with Ollama HTTP client
  - `describe_image(&self, image_base64: &str, prompt: &str) -> Result<String>`
  - Calls `POST http://localhost:11434/api/generate` with model=qwen3-vl
- [ ] Create `VisionProvider` trait in `crates/core/src/traits.rs`
- [ ] Wire into agent loop - detect image in context, call vision provider

#### Dashboard
- [ ] Image upload/attach in chat input
- [ ] Image preview in message bubbles
- [ ] Vision description in streaming response

#### Config
- [ ] `vision_model` already in `savant.toml`
- [ ] Add `vision_provider` field to `agent.json`

---

## Phase 6: Tool Gap Analysis (FUTURE)

Based on "Ranking AI Agent Tool Calls By Impact":

| Rank | Tool | Priority | Notes |
|------|------|----------|-------|
| 6 | repo_map | HIGH | AST-based code graph, token-efficient codebase awareness |
| 8 | llm_task | HIGH | Schema coercion for deterministic output |
| 9 | lobster (workflow) | HIGH | Deterministic macro sequences |
| 10 | computer_use | MEDIUM | Needs vision model (qwen3-vl) first |
| 11 | thinking_levels | MEDIUM | Cognitive budget allocation per turn |
| 13 | task_create | MEDIUM | Async job state management |
| 15 | tool_loop_detection | HIGH | Needs proper circuit breaker, not just dedup |
| 18 | elevated_mode | HIGH | Destructive action gating + human approval |
| 19 | diffs | LOW | Visual change review for human-in-the-loop |

---

## Architecture Notes (from user)

- Savant is the "house" - other agents are extensions within her
- Hive mind with shared memory
- Master key system: OR_MASTER_KEY → derivative keys at runtime, never persisted
- CCT is protection for new sub-agents, NOT for Savant
- Ollama is the primary model backend (99% of users have it)
- Dashboard should control everything you can edit via files
- LEARNINGS.md is Savant's private diary - emergent behavior is the crown jewel

---

*This plan follows the AUTONOMOUS-WORKFLOW.md protocol. Updated after each task.*
